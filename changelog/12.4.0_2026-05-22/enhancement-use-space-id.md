Enhancement: Use spaceId

We now use the `spaceId` when matching the space for a resource instead of manually constructed `storageId` from the resource ID.

https://github.com/owncloud/web/pull/13577
