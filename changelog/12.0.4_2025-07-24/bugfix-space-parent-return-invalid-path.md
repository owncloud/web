Bugfix: Space parent return invalid path when parent is spaces' root

We've fixed the issue where space parent return invalid path when parent is spaces' root.

https://github.com/owncloud/web/pull/12836
