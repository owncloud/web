import { SettingsBundle, SettingsValue } from '../../helpers/settings'

export const useNotificationsSettings = () => {
  const getNotificationsSettings = (values: SettingsValue[], bundle: SettingsBundle) => {
    // TODO: process and return values
  }

  return { getNotificationsSettings }
}
