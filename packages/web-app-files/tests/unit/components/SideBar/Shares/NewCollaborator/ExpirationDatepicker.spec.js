describe('NewCollaborator ExpirationDatepicker', () => {
  it.todo('only gets displayed if share expiration is supported')
  describe('when no expiration date is present', () => {
    it.todo('renders a button to open the datepicker and set an expiration date')
  })
  describe('when an expiration date is present', () => {
    it.todo('renders a button to open the datepicker and edit the expiration date')
    it.todo('renders a button to remove the expiration date')
    it.todo('clicking the remove-expiration-date button emits an event')
  })
})
